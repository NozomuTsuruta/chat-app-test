import React from 'react'
import {HeaderContainer,HeaderTitle} from './Header-Styles'

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Chat App</HeaderTitle>
    </HeaderContainer>
  )
}

export default Header
